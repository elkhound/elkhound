language: python

python:
  - "3.5"
  - "3.6"
  - "nightly"

install:
  - pip install --user -r requirements.txt
  - pip install --user .

script: nosetests --where test --with-xunit

branches:
  only:
    - master
